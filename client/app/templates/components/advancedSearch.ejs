<div class="advancedSearchView">
  <% if (numActive) { %>
    <div class="numActive"><%= numActive %> rules active</div>
  <% } %>
  <div class="rulesContainer"/>
  <a href="#" data-action="add-rule">add rule</a>
  <a href="#" class="pull-right" data-action="clear-all">clear all</a>
</div>
